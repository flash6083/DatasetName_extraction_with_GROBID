from datasetExtract import extractFromSentence
from datasetExtract import extractNamesFromPDF,extractNameAndContextFromPDF

print("Extraction starting\n")

#sample_input = "We trained binary neural network (BNN)10 classifiers on the MNIST database to recognize digits (0 to 9) using the pytorch implementation11 of the Binary-Backpropagation."

# 1) Extracts dataset name from a sentence

#input_data = input('Enter your Sentence that is to be searched for a dataset name: \n')
#extractFromSentence(input_data)

###################################################################################

#pdf_name = 'msmarco.pdf'
#pdf_path = "C:\\Users\\sayak\\Desktop\\Python_Resources\\msmarco.pdf"

# 2) Extracts just the dataset names from the PDF

#extractNamesFromPDF(pdf_name,pdf_path) 

# 3) Extracts the name and context of dataset from PDF

#extractNameAndContextFromPDF(pdf_name,pdf_path) 

